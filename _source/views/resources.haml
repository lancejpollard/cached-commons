%section#main.main{:role => :main}
  .body
    .frame
      - site.find_posts_by_category("libraries").each do |post|
        %article.post
          %h3= post.title
          .content
            :textile
              #{post.read}
  .links
    %a{:href => c("links.website")} Viatropos